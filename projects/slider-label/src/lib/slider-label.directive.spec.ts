import { SliderLabelDirective } from './slider-label.directive';

describe('SliderLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderLabelDirective();
    expect(directive).toBeTruthy();
  });
});
