import { TreeGridCellDirective } from './tree-grid-cell.directive';

describe('TreeGridCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeGridCellDirective();
    expect(directive).toBeTruthy();
  });
});
